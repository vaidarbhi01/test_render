# Important notes
* Mustache is a logic-less templating engine that works great with Node.js for rendering HTML (or other text formats) from data.
* .render take 2 arguments ::: 1) template 2) data.
* For project generate automatically install `npm install turbo-cli -g` , where -g stands for globally installed in your system.
